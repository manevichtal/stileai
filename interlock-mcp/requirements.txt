# Official MCP Python SDK. Pinned below 2.0 because the v2 line is a pre-release
# with breaking changes; v1.x is the stable, officially maintained line.
mcp>=1.27,<2
PyYAML>=6.0
httpx>=0.27          # used by the api (dashboard) store; also an MCP SDK dep
cryptography>=42
